#include <iostream>
//#include "Memoria.h"
#include <algorithm>

using namespace std;

int main() {
string commandInput;
if(commandInput.find_first_not_of(' ') != std::string::npos)
{
    //parse(commandInput);
}  

}